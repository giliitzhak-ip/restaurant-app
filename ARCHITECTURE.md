# GET SERVICE — ארכיטקטורה

> מסמך זה מתאר את השכבות, הזרימות המרכזיות וההחלטות מאחוריהן.
> לסכימת המסד ראה [`DATABASE_SCHEMA.md`](./DATABASE_SCHEMA.md).

---

## 1. עקרונות

1. **המסד הוא גבול האבטחה.** RLS פעיל בכל טבלה. ה-UI וה-API מסננים לנוחות;
   המסד מסנן לאמת. אם מישהו ירוץ עם ה-anon key מול ה-REST API ישירות, הוא לא
   יראה יותר ממה שמותר לו.
2. **לוגיקה עסקית מופרדת מ-UI.** חישוב עמלה, דירוג התאמות ומכונת המצבים של
   העבודה הם פונקציות טהורות ב-`lib/services/` — נבדקות ביחידה, חסינות לשינויי UI.
3. **כל אינטגרציה חיצונית היא interface + adapter.** תשלומים, מפות, התראות וסיווג
   AI — לכל אחד ממשק ומתאם Mock שעובד בלי מפתח. החלפת ספק היא מתאם חדש, לא שכתוב.
4. **תצורה במסד, לא בקוד.** עמלות, משקלי התאמה, רדיוסי חיפוש, timeouts ומדיניות
   ביטול יושבים בטבלת `settings` וניתנים לשינוי מפאנל הניהול.
5. **Push ולא polling.** סטטוס עבודה, הצעות, צ׳אט ומיקום מגיעים דרך Supabase
   Realtime. אין setInterval שמושך נתונים.

---

## 2. שכבות

```
┌─────────────────────────────────────────────────────────────────┐
│  app/                                                            │
│  Server Components (קריאה) · Client Components (אינטראקציה)      │
│  /  ·  /login  ·  /app  ·  /provider  ·  /admin  ·  /legal       │
└───────────────┬──────────────────────────────┬──────────────────┘
                │ fetch                        │ Supabase Realtime
┌───────────────▼──────────────┐   ┌───────────▼──────────────────┐
│  app/api/**  (Route Handlers)│   │  hooks/use-realtime           │
│  Zod validation              │   │  (RLS חל גם על מנויים)        │
│  requireSession / requireRole│   └───────────────────────────────┘
│  rate limiting               │
└───────────────┬──────────────┘
                │
┌───────────────▼─────────────────────────────────────────────────┐
│  lib/services/                                                   │
│  matching/  fees/  payments/  notifications/  maps/  ai/         │
│  jobs/ (workflow, anti-fraud)  settings/  catalogue/             │
└───────────────┬─────────────────────────────────────────────────┘
                │
┌───────────────▼─────────────────────────────────────────────────┐
│  Supabase — Postgres + PostGIS · Auth · Storage · Realtime        │
│  RLS · טריגרים · find_nearby_providers()                          │
└──────────────────────────────────────────────────────────────────┘
```

### שני לקוחות Supabase

| לקוח | מתי | RLS |
| --- | --- | --- |
| `getServerSupabase()` | כל פעולה בשם המשתמש | חל |
| `getServiceSupabase()` | פעולות שהפלטפורמה מבצעת: שידור עבודה, תשלומים, התראות, פעולות מנהל | **עוקף** |

ה-service client מוחזר רק דרך `requireServiceClient()`, ורק אחרי ש-`requireRole`
כבר אישר את הקורא. שימוש בו בלי בדיקה מוקדמת הוא באג אבטחה, ולכן הוא לעולם לא
נוצר ישירות ב-route handler.

---

## 3. תפקידים והרשאות (RBAC)

התפקיד נשמר ב-`public.users.role` ונקבע בטריגר `handle_new_user` בעת ההרשמה.
הטריגר יודע לייצר `customer` או `provider` בלבד — **`admin` לא ניתן להשגה דרך
הרשמה**, גם אם הלקוח שולח אותו במטא-דאטה.

האכיפה מתבצעת בשלוש שכבות בלתי תלויות:

| שכבה | מה היא עושה |
| --- | --- |
| `proxy.ts` | מרענן את עוגיית ה-session ומפנה אנונימי ל-`/login`. **לא** גבול אבטחה. |
| Layout של כל אזור | `getSessionContext()` + בדיקת תפקיד; לקוח שינסה `/admin` יופנה הביתה. |
| `requireRole()` ב-API + RLS | האכיפה האמיתית. גם אם ה-UI נעקף, השאילתה לא תחזיר כלום. |

---

## 4. מנוע ההתאמה

### 4.1 גילוי (Discovery)

`find_nearby_providers(category_id, lat, lng, radius_km, service_id, max_results)`
היא פונקציית SQL עם PostGIS שמסננת בלבד:

- בעל מקצוע `verified` ומשתמש `active`
- שייך לקטגוריה המבוקשת
- המרחק מחושב מהמיקום החי (אם דווח לאחרונה) או ממרכז אזור השירות
- נכלל אם הוא בתוך הרדיוס **או** אם אזור השירות שהגדיר מכסה את מיקום העבודה

### 4.2 הרחבת רדיוס הדרגתית

`discoverProviders()` מתחיל ב-5 ק״מ. אם לא הצטברו `minimum_providers` (ברירת
מחדל 3), הוא מרחיב ל-10 ואז ל-20. השלבים מוגדרים ב-`settings.matching.radius_steps_km`.

### 4.3 ניקוד

`scoreCandidate()` מנרמל כל רכיב ל-0–1 ומשקלל לפי `settings.match_weights`:

| רכיב | ברירת מחדל | איך מנורמל |
| --- | --- | --- |
| `distance` | 30% | דעיכה ליניארית לאורך הרדיוס הנוכחי |
| `rating` | 20% | 1★→0, 5★→1, ממותן לפי מספר הביקורות |
| `availability` | 15% | Toggle + טריות המיקום; לא-זמין = 0 לעבודה דחופה |
| `category_match` | 15% | התאמת שירות מדויק מול קטגוריה בלבד |
| `response_speed` | 10% | הפוך לזמן תגובה ממוצע, רווי ב-30 דק׳ |
| `completed_jobs` | 10% | לוגריתמי — 50 עבודות = ציון מלא |

ואז מתווספים מתאמים אדיטיביים: בונוס מועדף, בונוס אזור שירות מוצהר, התאמה
לתקציב, וקנס על שיעור ביטולים. התוצאה היא 0–100.

**הנוסחה לא נחשפת ללקוח.** `settings` נגישה למנהלים בלבד (RLS), והשרת קורא אותה
דרך ה-service client. הלקוח רואה מיון "מומלץ" — לא משקלים.

### 4.4 שידור

`broadcastJob()` כותב `job_assignments`. זו לא רק רשומת ניקוד — **זו שורת
ההרשאה**: מדיניות ה-RLS של `jobs` נותנת לבעל מקצוע לראות עבודה רק אם קיימת לו
שורת assignment עבורה. אין שידור → אין גישה.

---

## 5. מחזור חיי עבודה

```
REQUESTED → SEARCHING → OFFERS_RECEIVED → PROVIDER_SELECTED
    → PROVIDER_ON_THE_WAY → ARRIVED → IN_PROGRESS → COMPLETED
                    ↘ CANCELLED            ↘ DISPUTED
```

מכונת המצבים ב-`lib/services/jobs/workflow.ts` מגדירה גם **מי** רשאי לבצע כל
מעבר. בעל מקצוע לא יכול לשבץ את עצמו; לקוח לא יכול לסמן "הושלם"; אף אחד לא יכול
לקפוץ מ-`requested` ל-`completed`. כל שינוי סטטוס נרשם אוטומטית ב-
`job_status_history` על ידי טריגר, ולא על ידי הקוד שמעדכן.

`POST /api/jobs/:id/complete` מבצע סיום **וגבייה באותה בקשה** — כדי שלא תיווצר
עבודה שהושלמה בלי תשלום או תשלום בלי עבודה.

---

## 6. תשלומים

```
בחירת הצעה → authorize (תפיסת מסגרת)
סיום עבודה → capture → רישום platform_fee → סכום נטו לבעל המקצוע
ביטול      → refund / cancel של ההרשאה
```

- הסכום נלקח **תמיד** משורת ההצעה במסד, לא מגוף הבקשה.
- הפיצול מחושב מחדש בשרת בכל שלב (`lib/services/fees`).
- `payments.fee_rule_snapshot` שומר את הכלל שהופעל, כדי ששינוי עתידי בהגדרות לא
  ישנה עסקאות היסטוריות.
- אילוץ במסד (`payments_split_balances`) מוודא ש-`platform_fee + provider_payout`
  שווה ל-`amount` בדיוק.
- `MockPaymentAdapter` מממש את כל מכונת המצבים בזיכרון, כך שכל הזרימה ניתנת
  להרצה ולבדיקה בלי מפתח.

### כללי עמלה

סדר קדימויות: override לבעל מקצוע → override לקטגוריה → מדרגת סכום → ברירת מחדל.
לאחר מכן הידוק ל-minimum/maximum, והגבלה שהעמלה לעולם לא עולה על סכום העבודה.
**בעל מקצוע לא יכול לעקוף עמלה**: ה-overrides הם הגדרת מנהל, והחישוב כולו בשרת.

---

## 7. Realtime

| ערוץ | טבלה | מי מנוי |
| --- | --- | --- |
| סטטוס עבודה | `jobs` | לקוח ובעל המקצוע המשובץ |
| הצעות | `job_offers` | לקוח העבודה |
| שידור חדש | `job_assignments` | בעל המקצוע |
| צ׳אט | `messages` | שני הצדדים בעבודה |
| מיקום חי | `provider_locations` | הלקוח, רק בזמן עבודה פעילה |
| התראות | `notifications` | הנמען |

RLS חל גם על מנויי Realtime, כך שמנוי לא יכול לקבל שורה שאסור לו לקרוא.

---

## 8. פרטיות מיקום

- מיקום בעל מקצוע נשמר רק כאשר הוא מסומן **זמין** או שיש לו עבודה פעילה.
  `PUT /api/provider/location` בודק זאת ומוחק את השורה אחרת.
- מדיניות ה-RLS על `provider_locations` נותנת ללקוח לראות מיקום רק כאשר בעל
  המקצוע משובץ לעבודה שלו **ובסטטוס פעיל**.
- גישת Geolocation בדפדפן נדרשת רק לאחר פעולה מפורשת של המשתמש; סירוב הוא מסלול
  לגיטימי — מזינים כתובת ידנית וה-geocoding מתבצע בשרת.

---

## 9. אחסון קבצים

| Bucket | ציבורי | תוכן |
| --- | --- | --- |
| `avatars` | כן | תמונות פרופיל |
| `provider-gallery` | כן | גלריית עבודות |
| `job-media` | לא | תמונות וסרטוני עבודה |
| `chat-media` | לא | תמונות בצ׳אט |
| `provider-documents` | **לא** | מסמכי אימות |

מוסכמת הנתיב היא `<user_id>/<uuid>.<ext>` — הבעלות מוכחת מהנתיב, וזה מה שמדיניות
ה-Storage בודקת. השרת מנפיק signed upload URL; הקבצים עצמם לא עוברים דרכו.

---

## 10. התראות

`notify()` מפזר אירוע לערוצים שמנהל הפעיל עבורו. שורת `in_app` נכתבת תמיד (היא
מרכז ההתראות והפיד בזמן אמת); ערוצים חיצוניים הם best-effort ולעולם לא מפילים
את הפעולה העסקית. שעות שקט חוסמות ערוצים חיצוניים ומשאירות את השורה הפנימית.

---

## 11. i18n ו-RTL

עברית היא מקור האמת (`lib/i18n/dictionaries/he.ts`), והטיפוס `Dictionary` נגזר
ממנה. `en`, `ar`, `ru` הם מילונים חלקיים שממוזגים מעל עברית, כך ששפה מתורגמת
חלקית לא מציגה מחרוזת ריקה. השפה נשמרת בעוגייה ונקראת ב-root layout שקובע
`lang` ו-`dir` — מעבר לאנגלית הופך את המסמך כולו ל-LTR, וערבית נשארת RTL.

---

## 12. עמידות ומצב הדגמה

`lib/env.ts` לא זורק בזמן import ומזהה placeholders מ-`.env.example`. כל אינטגרציה
שואלת אם היא מוגדרת ונופלת ל-Mock אחרת. התוצאה: `npm install && npm run dev`
נותן אפליקציה שעובדת, עם הודעה מפורשת על מה שאינו מחובר — במקום קריסה.

---

## 13. מוכנות לעתיד

- **מנויים / זכיינות / פרסום** — `settings` כבר גנרי, ו-`platform_fees` מופרד
  מ-`payments`, כך שמודל חיוב נוסף לא דורש שינוי סכימה מרכזי.
- **AI** — `AIJobClassifier` הוא interface; `setJobClassifier()` מחליף מימוש
  בלי לגעת באשף.
- **ספק סליקה ישראלי** — `IsraeliPaymentAdapter` כבר קיים עם זרימת J5/J4; חסרה
  רק נקודת קצה ומפתח.
- **אפליקציה נייטיב** — כל הלוגיקה מאחורי API עם חוזה Zod; קליינט נוסף אפשרי.
