# STANGA — תוכנית שלב ג׳ (גרסה 0.3.0)

## המטרה

אונליין 1 נגד 1 אמיתי: שרת סמכותי, חדר פרטי עם קוד וקישור הזמנה,
חיפוש משחק, חיבור מחדש, ו־Docker. בלי לשבור את המצבים הקיימים.

## הגדרת ההיקף

שלב זה **אינו** כולל 2 נגד 2, Party, בוטים, Quick Chat או סטטיסטיקות קבוצתיות.
אלה שייכים לשלב ד׳, והארכיטקטורה כאן נבנית כדי לקבל אותם בלי שכתוב.

## החסם שנבדק ראשון

לפני כל שורת קוד נבדק הדבר היחיד שיכול היה להפיל את כל השלב:
**האם הסימולציה יכולה לרוץ ב־Node ללא דפדפן?**

נבדק בפועל — `@babylonjs/havok` + `NullEngine` + `PhysicsAggregate` עובדים ב־Node
(כדור נופל, מתנגש ברצפה ונעצר). המשמעות: **השרת מריץ את אותו `MatchEngine`
של הלקוח**, ולא מימוש פיזיקה שני שייפרד ממנו עם הזמן.

התנאי היחיד: הפרדת גופי הפיזיקה מהמראה — בדיוק החסם שתועד ב־`multiplayer-readiness.md`.

## מנות העבודה

| #   | מנה               | תוצר                                                                         |
| --- | ----------------- | ---------------------------------------------------------------------------- |
| 1   | הפרדת Body מ־View | `*Body` רץ ב־Node, `*View` נשאר בלקוח                                        |
| 2   | סימולציה חסרת־ראש | `HeadlessMatch` — יצירת סצנה, פיזיקה ומנוע ללא מראה                          |
| 3   | פרוטוקול וסכמות   | `protocol.ts` משותף, Colyseus schema, `protocolVersion`                      |
| 4   | שרת               | `BaseOnlineMatchRoom` → `OnlineOneVsOneRoom`, לולאה סמכותית, health check    |
| 5   | לקוח רשת          | `RoomClient`, `NetworkController`, prediction, reconciliation, interpolation |
| 6   | ממשק              | תפריט אונליין, חדר פרטי, קוד וקישור, חיפוש, ping, חיבור מחדש                 |
| 7   | חיבור מחדש        | grace period, snapshot מלא, החזרת שליטה בטוחה                                |
| 8   | אבטחה             | אימות פקודות, rate limit, origins, דחיית לקוח ישן                            |
| 9   | Docker ופריסה     | image, health check, docker-compose, תיעוד sticky sessions                   |
| 10  | בדיקות            | יחידה, אינטגרציה, E2E בשני דפדפנים, עומס                                     |
| 11  | תיעוד             | פרוטוקול, ארכיטקטורה, פיזיקה ברשת, פריסה, README, CHANGELOG                  |

## החלטות ארכיטקטורה

1. **ליבה משותפת מהיום הראשון.** `BaseOnlineMatchRoom` ו־`MatchConfig` נכתבים
   כך ש־2×2 יהיה תוספת של קונפיגורציה וחדר, לא מערכת שנייה.
   `MatchConfig` כולל כבר `mode`, `maxPlayers`, `playersPerTeam`.
2. **השרת מריץ את `MatchEngine` הקיים**, לא עותק. כל שינוי בחוקים חל על שניהם.
3. **הלקוח אינו מריץ פיזיקה במצב אונליין.** הוא מנבא את תנועת השחקן המקומי
   בלבד, ומצייר את היריב ואת הכדור מתוך אינטרפולציה בין snapshots. זהו לקוח
   שרת־סמכותי סטנדרטי, והוא היחיד שאינו מתפצל מהשרת עם הזמן.
4. **`NetworkController` מממש את `PlayerController`** שנבנה בשלב ב׳ — בדיוק
   נקודת ההחלפה שתוכננה שם.
5. **קוד ההזמנה אינו reconnect token.** שני מזהים נפרדים לחלוטין.
6. **אין state גלובלי בתהליך.** כל מצב חי בחדר, כדי שהוספת Redis ו־scaling
   בהמשך לא תדרוש שכתוב.

## מה נמסר בפועל (0.3.0)

כל 11 המנות בוצעו. שלוש הערות על הפער בין התוכנית למימוש:

1. **הלקוח כן מריץ פיזיקה.** התוכנית אמרה שלא. בפועל, ניבוי של תנועת השחקן
   המקומי בלי פיזיקה היה דורש מימוש שני של קוד התנועה — בדיוק הדבר שהשלב הזה
   נבנה כדי למנוע. במקום זה הלקוח מריץ את אותו `MatchEngine` במצב `mirrored`,
   שבו הוא מנבא אך אינו מחליט: הניקוד, השעון, השלב והשערים מגיעים מהשרת. אין
   rollback, וזה מתועד במפורש ב־[`physics-networking.md`](physics-networking.md).
2. **חדר פרטי נפתר דרך HTTP ולא דרך `filterBy`.** `filterBy` היה חושף את קודי
   ההזמנה לכל מי שמבקש רשימת חדרים. במקום זה החדר מוגדר `private` אמיתי,
   והקוד נפתר ב־`GET /invite/:code` מול Colyseus Presence.
3. **שני באגים בסימולציה נמצאו תוך כדי** — `reset()` ששיגר גופים במקום להציב
   אותם, וסיוע השליטה בכדור שביטל בעיטות. שניהם היו קיימים מ־0.1.0, ובלעדיהם
   שער לא היה נספר. יש להם בדיקות רגרסיה.

### מה במפורש לא נמסר

- 2 נגד 2, Party, בוטים, Quick Chat, סטטיסטיקות קבוצתיות — שלב ד׳.
- פריסה מרובת־תהליכים: מוכנה ארכיטקטונית (Presence, Driver), לא נבדקה.
- בניית ה־Docker image: לא בוצעה בסביבת הפיתוח (אין Docker daemon).
