# GET SERVICE — תוכנית הפרויקט

**מוצר:** Marketplace להזמנת בעלי מקצוע בזמן אמת.
**סלוגן:** המקצוען הנכון. בדיוק כשצריך.
**מצב:** MVP עובד — בנוי, נבדק, עובר lint/typecheck/build.

---

## 1. היקף ה-MVP

| תחום | מה נכלל |
| --- | --- |
| לקוח | הרשמה, בית עם קטגוריות וחיפוש, אשף בקשה ב-9 שלבים, קבלת הצעות, בחירת בעל מקצוע, תשלום, מעקב חי, צ׳אט, דירוג, מועדפים, היסטוריה, פרופיל |
| בעל מקצוע | הרשמה, אשף onboarding ב-8 שלבים, אימות מסמכים, toggle זמינות, פיד עבודות, שליחת הצעה עם תצוגת נטו, ניווט, עדכון סטטוס, סיום, הכנסות, פרופיל |
| מנהל | KPIs, אימות בעלי מקצוע, ניהול משתמשים, עבודות, עסקאות, מודרציית דירוגים, תלונות, קטלוג, הגדרות פלטפורמה |
| פלטפורמה | מנוע התאמה, גילוי גיאוגרפי עם הרחבת רדיוס, עמלות מדורגות, Realtime, התראות רב-ערוציות, anti-fraud בסיסי, i18n/RTL, מסמכים משפטיים |

---

## 2. שלבי הבנייה — מה בוצע

| # | שלב | סטטוס |
| --- | --- | --- |
| 1 | סריקת ה-repo; ארכוב אב-הטיפוס הקודם ל-`legacy/` | ✅ |
| 2 | Next.js 16 + TypeScript + Tailwind + ESLint + Vitest | ✅ |
| 3 | סכימת מסד: 29 טבלאות, enums, אינדקסים, אילוצים | ✅ |
| 4 | 8 מיגרציות Supabase כולל PostGIS ו-`find_nearby_providers` | ✅ |
| 5 | Row Level Security על כל טבלה + טריגרי הגנה על עמודות | ✅ |
| 6 | Auth: אימייל/סיסמה, OTP בטלפון, callback, provisioning בטריגר | ✅ |
| 7 | RBAC בשלוש שכבות: proxy, layout, API + RLS | ✅ |
| 8 | UI לקוח — 9 מסכים | ✅ |
| 9 | UI בעל מקצוע — 7 מסכים | ✅ |
| 10 | UI מנהל — 9 מסכים, desktop-first | ✅ |
| 11 | מערכת עבודות: מכונת מצבים, היסטוריה, ביטול, תלונות | ✅ |
| 12 | מערכת הצעות: שליחה, ביטול, קבלה, דחייה, תוקף | ✅ |
| 13 | מנוע התאמה: 6 רכיבי ניקוד + מתאמים, משקלים מהמסד | ✅ |
| 14 | Realtime: סטטוס, הצעות, צ׳אט, מיקום, התראות | ✅ |
| 15 | צ׳אט לעבודה: טקסט + תמונה, RLS לשני הצדדים | ✅ |
| 16 | דירוגים: כללי + 4 קריטריונים, אגרגציה בטריגר, מודרציה | ✅ |
| 17 | הפשטת תשלומים: Mock / Stripe / ספק ישראלי, authorize→capture | ✅ |
| 18 | הפשטת התראות: in-app / email / SMS / push / WhatsApp | ✅ |
| 19 | Seed: 11 לקוחות, 20 בעלי מקצוע, 50 עבודות, הצעות, תשלומים, דירוגים | ✅ |
| 20 | 111 בדיקות ב-9 קבצים — יחידה + אינטגרציה | ✅ |
| 21–26 | lint · typecheck · tests · production build — כולם ירוקים | ✅ |

---

## 3. אימות סופי

| בדיקה | תוצאה |
| --- | --- |
| `npm install` | ✅ ללא פגיעויות |
| `npm run lint` | ✅ 0 שגיאות, 0 אזהרות |
| `npm run typecheck` | ✅ |
| `npm run test` | ✅ 111/111 |
| `npm run build` | ✅ 62 מסלולים, ללא אזהרות |
| `npm run start` + smoke test | ✅ כל האזורים מחזירים 200 |
| סכימת מסד | ✅ 8 מיגרציות הוחלו על PostgreSQL 16 + PostGIS ללא שגיאות |
| אבטחת מסד | ✅ 13 טענות RLS/טריגרים עוברות מול מסד אמיתי |
| RTL ועברית | ✅ `dir="rtl"` ברמת המסמך, מספרים מבודדים ל-LTR |
| סודות בקוד | ✅ אין — הכול דרך `lib/env.ts` |

---

## 4. מה מכוסה בבדיקות

| קובץ | מה נבדק |
| --- | --- |
| `fees.test.ts` | מדרגות, min/max, overrides, שקילות הפיצול, תצוגת לקוח מול בעל מקצוע |
| `match-engine.test.ts` | כל רכיב ניקוד, דירוג, דטרמיניזם, הרחבת רדיוס, נרמול משקלים |
| `job-workflow.test.ts` | מעברים חוקיים, הרשאת שחקן, דמי ביטול |
| `validation.test.ts` | סכימות Zod — כולל שהלקוח לא יכול לבקש תפקיד `admin` |
| `payment-flow.test.ts` | authorize → capture → refund, אידמפוטנטיות, רישום עמלה |
| `job-lifecycle.test.ts` | הזרימה המלאה: בקשה → שידור → הצעה → קבלה → התקדמות → סיום → דירוג |
| `ai-classifier.test.ts` | סיווג טקסט חופשי, דחיפות, גבולות הקטלוג |
| `notifications.test.ts` | שעות שקט, תבניות לכל אירוע |
| `geo.test.ts` | haversine, ETA, bounding box |
| `supabase/tests/rls-verification.sql` | הרשאות ברמת השורה, עמודות מוגנות, טריגרים ואילוצים — מול מסד אמיתי |

---

## 5. מה דורש מפתח כדי לעבור למצב חי

| רכיב | מה חסר | מה קורה בינתיים |
| --- | --- | --- |
| Supabase | URL + מפתחות + `db push` | מצב הדגמה עם קטלוג מקומי |
| Stripe / סליקה ישראלית | `PAYMENT_SECRET_KEY` (+ endpoint לישראלי) | `MockPaymentAdapter` מריץ את כל מכונת המצבים |
| Google / Mapbox | `MAPS_API_KEY` | geocoding מקומי לערים בישראל, ETA מוערך, תרשים מיקום |
| SMS / Email / Push / WhatsApp | מפתחות ספק | לוג מובנה לקונסול; שורת `notifications` נשמרת תמיד |
| OTP בטלפון | ספק SMS בפרויקט Supabase | מסך קיים ומדווח בבירור שהשליחה נכשלה |

הארכיטקטורה זהה בשני המצבים — רק המתאם מתחלף.

---

## 6. מה מחוץ להיקף (ומוכן להרחבה)

לפי הבריף, לא נבנו כעת: מנויים, זכיינות, marketplace פרסום, עוזר AI, חוזי
Enterprise. הארכיטקטורה מאפשרת אותם: `settings` גנרי, `platform_fees` מופרד
מ-`payments`, ו-`AIJobClassifier` הוא interface שניתן להחליף בקריאה אחת.

---

## 7. צעדים מומלצים להמשך

1. **תשתית:** חיבור פרויקט Supabase אמיתי, הרצת מיגרציות, הפעלת גיבויים.
2. **סליקה:** בחירת ספק ישראלי, מילוי `IsraeliPaymentAdapter` וחיבור webhook.
3. **משפטי:** העברת חמשת מסמכי הטיוטה לעורך דין לפני עלייה לאוויר.
4. **Rate limiting מבוזר:** החלפת המימוש בזיכרון ב-Redis/Postgres לריצה מרובת מופעים.
5. **עבודות רקע:** תזמון פקיעת הצעות והרחבת רדיוס אוטומטית (Supabase Cron).
6. **תשלומים לבעלי מקצוע:** חיבור payouts בפועל (`transaction_type.payout` כבר קיים).
7. **תצפיתיות:** Sentry או מקביל, ומעקב אחרי שיעור התאמה וזמן לתגובה ראשונה.
8. **נגישות:** ביקורת ידנית עם קורא מסך בעברית בנוסף לבדיקות שכבר בוצעו.
